#pragma once

// сортировка list методом merge.
list::List *mergeSort(list::List *list);
listInMassive::List *mergeSort(listInMassive::List *list);


// поиск длинны листа list.
//int lengthList(list::List *list);
// сортировка слиянием листа list. 
// проверка больше ли первое, чем второе.
//bool isFirstMax(list::Position first, list::Position second);


